import React from 'react'
import {useSelector} from 'react-redux'

const getDataredux = () => {
    const data_boys = useSelector((state) => state)
  return (
    <div>g

    </div>
  )
}

export default getDataredux